generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Kegiatan {
  id          Int      @id @default(autoincrement())
  judul       String
  deskripsi   String?
  tanggal     DateTime
  pembuat     String
  createdAt   DateTime @default(now())
}